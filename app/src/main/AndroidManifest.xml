<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.FM_SHOP">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClientAssignment"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.MessageActivity"></activity>
        <activity android:name=".uiAdmin.AddProductActivity" />
        <activity android:name=".uiAdmin.AdminActivity" />
        <activity android:name=".uiUser.DetailProductActivity" />
    </application>

</manifest>